module.exports = {
  testEnvironment: "node",
  verbose: true, // report each test
  forceExit: true, //exit test per run
  // setupFilesAfterEnv: ["C:/Codes/berry-nails-web/backend/test/setupTests.js"],
  moduleFileExtensions: ["js", "json"], //Ensures Jest loads files properly.
};
